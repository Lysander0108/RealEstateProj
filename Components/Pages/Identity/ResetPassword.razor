@page "/ResetPassword"

<div class="card-wrapper">
    <InputText @bind-Value="email">enter email </InputText>
    <InputText type="password" placeholder="Password" @bind-Value="NewPassword"> enter new password</InputText>
    <InputText type="password" placeholder="Password" @bind-Value="NewPassword1"> enter again</InputText>
    @if (NewPassword != NewPassword1 && NewPassword != "")
    {
        <span style="color:darkred">Password Missmatch</span>
    }

    <button></button>

</div>

@code {
    //TODO add email ver somehow
    string email = "";
    string NewPassword = "";
    string NewPassword1 = "";
}
